import React from 'react'
import headerStyles from './header.module.css'

const Header = () => (
  <header className={headerStyles.header}>
    <div className={headerStyles.headerContent}></div>
  </header>
)

export default Header
